{
  "name": "forty-two-watts",
  "version": "0.127.0",
  "private": true,
  "type": "module",
  "description": "Unified Home Energy Management System — version metadata only; the host is Go and the mathematical planner is Python/CVXPY. Changesets reads `version` from this file to drive the release tag.",
  "scripts": {
    "test": "node --test 'web/**/*.test.mjs'"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10"
  }
}
