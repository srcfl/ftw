---
"ftw": minor
---

Keep history as chart buckets and the energy ledger in one SQLite file. On boot, fold older cold bucket files into hourly rows, then remove those files and the retired raw database.
