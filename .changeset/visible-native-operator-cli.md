---
"ftw": minor
---

Add an operator CLI that shows full backup and native update progress, copies a verified backup to another computer, and pilots a guarded move from an older native systemd install to one exact 0.x release.
