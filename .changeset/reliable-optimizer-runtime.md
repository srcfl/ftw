---
"ftw": patch
---

Use the optimizer sidecar without a hidden Python process in Compose, keep its first failure visible when a development fallback also fails, and use the 30-second Core timeout throughout the backend.
