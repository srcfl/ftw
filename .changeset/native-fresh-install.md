---
"ftw": minor
---

Install a verified native 0.x release on a fresh Linux host from an exact tag.
The old one-line Docker installer is retired; existing installations remain
untouched until the guided migration is ready.
