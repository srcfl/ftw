---
"ftw": patch
---

Teach the ESPHome DSMR meter driver to read per-phase current, voltage and power from default ESPHome name-derived object_ids (for example `current_phase_1`) and delivered/returned totals while keeping Sourceful Zap compatibility.
