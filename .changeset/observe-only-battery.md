---
"ftw": minor
---

Add `observe_only` on battery drivers: keep structured telemetry and dashboard visibility while excluding the driver from dispatch, MPC, and watchdog commands. For sites where a retailer VPP owns battery actuation.
