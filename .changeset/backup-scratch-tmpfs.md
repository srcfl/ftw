---
"ftw": patch
---

A live backup writes its temporary database in memory when that fits, so scratch commits on a Raspberry Pi no longer hold up a durable charging-goal save. The synced archive is unchanged.
