---
"ftw": patch
---

Allow a driver update when the device first known by MAC (or endpoint) starts reporting its serial. That is the same hardware becoming better identified, not a swap; a different serial or MAC still rolls the update back.
