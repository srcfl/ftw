---
"ftw": minor
---

Add a read-only FTW storage inventory and dry-run disk-budget advisor to Settings → System and the local diagnostics API.

The inventory reports SQLite page and WAL usage, Parquet, snapshots, full backups and filesystem headroom without changing retention or stored data.
