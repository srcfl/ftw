---
"ftw": patch
---

Pin Energyplan 0.4.0. The worker keeps a proven incumbent when a later dual or MIP step fails, reports Feasible instead of Optimal when the search did not close, and stays alive on hostile protocol input. Handshake now advertises demand_charges. Core still does not send weekday 06–20 windows as hours[], so field plans do not change until that expansion lands.
