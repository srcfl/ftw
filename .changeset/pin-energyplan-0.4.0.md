---
"ftw": minor
---

Pin Energyplan 0.4.0 and send its planner contract without öre field names. Slot prices go as price_per_kwh / spot_per_kwh in the same minor currency units as before. When a weekday 06–20 demand charge is configured, Core expands local clock hours (including DST), applies VAT, and attaches already-measured month hours. Empty demand_price_per_kw leaves planning unchanged.
