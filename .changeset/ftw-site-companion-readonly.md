---
"ftw": minor
---

Add the versioned, opt-in FTW Site Companion read-only contract: a dedicated P-256 identity, short-lived signed pairing proof with optional Sourceful Zap site anchor, and signed status/health/plan snapshots that deliberately exclude raw energy telemetry and all control surfaces.
