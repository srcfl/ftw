---
"ftw": patch
---

Let a native Core stage and start a verified 0.x release, show its progress, and return to the previous same-schema binary. Keep data restore offline. Block 1.x and 2.x Core updates into another major release line while the guided migration path is built.
