---
"ftw": patch
---

Publish Linux arm64 and amd64 packages on beta as well as stable. Both use the same package builder and include Core, backup, web, drivers, Energyplan and the systemd service. New releases no longer build Windows packages.
