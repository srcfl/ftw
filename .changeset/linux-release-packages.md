---
"ftw": patch
---

Publish Linux arm64 and amd64 packages on beta as well as stable. Both use the same package builder and include Core, backup, web, drivers, Energyplan and the systemd service. Each package includes only its target Energyplan executable, with matching metadata and licenses. New releases no longer build Windows packages.
