---
"ftw": patch
---

`ftw status` no longer says a driver from the channel runs "until a release has
it" when the release already has the same version; it says the version is the
same as the release's.
