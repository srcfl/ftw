# Agent Artifacts

Archived planning documents, Goalbuddy state, and agent-generated specs.

These files are retained for traceability but are not operator docs,
contributor guides, or active implementation plans. Move current specs back
into the active docs tree only when they are intentionally maintained.

Generated `.goalbuddy-board/` static assets are intentionally not kept here;
they are local render output and ignored by `.gitignore`.
